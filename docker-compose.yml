version: "3.9"

services:
  state_api:
    build: ./state_api
    networks:
      - telescope
  webcam:
    build: ./webcam
    networks:
      - telescope
    ports:
      - 5001:5000
  website:
    build: ./website
    networks:
      - telescope
    ports:
      - 5000:5000

networks:
  telescope:
    driver: bridge
